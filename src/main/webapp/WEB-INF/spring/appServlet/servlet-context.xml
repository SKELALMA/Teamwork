<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.1.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />
	<resources mapping="/images/**" location="/resources/images/" />

	<!-- 외부에서 이미지 접근이 안된다 -->
	<resources mapping="/upload/**" location="/upload/"/>

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<context:component-scan base-package="com.woori.myhome" />
	
	<!-- 파일 업로드 객체 -->
	<beans:bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
      <beans:property name="maxUploadSize" value="20971520"></beans:property>   <!-- 파일의 크기를 20mb까지만 -->
      <beans:property name="maxInMemorySize" value="10485760"></beans:property>
   </beans:bean>
   
   <interceptors>
   		<interceptor>
 <!--  		<mapping path="/**/*"></mapping>  --> 
   				<mapping path="/**/w*"></mapping> <!-- w으로 시작하는 url -->
   			<mapping path="/**/modi*"></mapping> <!-- m으로 시작하는 url -->
   			<beans:bean id="loginInterceptor" class="com.woori.myhome.common.LoginInterceptor"/>
   		</interceptor>
   		
   		
   		<interceptor>
   			<mapping path="/admin/main/**"></mapping>
   			<mapping path="/admin/member/**"></mapping>
   			<mapping path="/admin/gallery/**"></mapping>
   			<mapping path="/admin/receipe/**"></mapping>
   			<mapping path="/admin/qnaboard/**"></mapping>
   			<mapping path="/admin/noticeboard/**"></mapping>
   			<beans:bean id="adminLoginInterceptor" class="com.woori.myhome.admin.AdminLoginInterceptor"/>
   		</interceptor>
   </interceptors>
	
</beans:beans>
